name: called

on:
  workflow_call:
    inputs:
      config-path:
        required: true
        type: string
    secrets:
      envPAT:
        required: true

jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    environment: production
    steps:
    - name: Step 1
      run: | 
        echo "Non-secret is ${{ inputs.config-path }}"
        echo "Secret is ${{ secrets.envPAT }}"
